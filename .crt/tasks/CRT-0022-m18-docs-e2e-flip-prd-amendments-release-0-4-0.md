---
id: CRT-0022
title: M18 — README for embedded mode, e2e axis flip, PRD amendments, v0.4 DoD, release 0.4.0
status: done
priority: normal
created: 2026-09-17T08:54:00+08:00
updated: 2026-09-21T10:05:00+08:00
url: null
route: null
session: null
tags: [m18, embedded, docs, e2e, release, f-107, f-108, f-109, f-110, prd-embedded]
files: [README.md, packages/server/README.md, docs/PRD.md, docs/PRD-providers.md, docs/PRD-setup.md, docs/PRD-embedded.md, CLAUDE.md, .claude/agents/prd-reviewer.md, packages/server/package.json, plugin/.claude-plugin/plugin.json, .claude-plugin/marketplace.json, package-lock.json, plugin/skills, packages/server/e2e, packages/server/playwright.config.ts, packages/server/test/readme.test.ts]
---

## Summary
Finish v0.4: rewrite the README around embedded mode (install, the four snippets, Production, What lands in your repo, Proxy mode, the redrawn diagram, troubleshooting with every new `crt:` line), flip the e2e primary servers to embedded mode, apply the PRD-embedded §9 amendments inline to the three earlier PRDs and to `CLAUDE.md` and the prd-reviewer, tick the §10 definition of done with evidence, bump to `0.4.0` (manifests, lockfile, seven skill pins), tag and release. `docs/PRD-embedded.md` §6.6, §6.7 (F-107, F-108, F-109, the M18 half of F-110), §9, §10.

## Context
After CRT-0019…0021 the code is embedded-by-default but the README still describes the proxy (`Script-tag fallback`, `How it works` with the proxy diagram), the e2e primary servers still run `crt proxy` with `baseURL` on the CRT port (PRD-embedded §13 decision 10), `docs/PRD.md` §5.2 still says "Why a proxy", and the skills pin `@0.3`. `test/readme.test.ts` is the doc test that greps for every `crt:` line and the Install block; `test/skill-pin.test.ts` enforces one version everywhere; `release.yml` publishes on a `v*` tag and Simon promotes the staged version on npm (CLAUDE.md Release line; his PowerShell 5.1 has no `&&`, so give him commands on separate lines). Depends on CRT-0019, CRT-0020, CRT-0021.

## Evidence
No page capture: created from `docs/PRD-embedded.md` milestone M18 by the planning session that wrote it.

## Ask
1. README (F-107): Install = machine block (unchanged) + the PRD-embedded §4 project block; "Add CRT to your app" with the four snippets verbatim and where each starts capturing (Vite/loader: before the app's first module; React/Next: after hydration; script tag: from the tag onward); "The loop" starts `npm run dev` → `crt` → browse your app; "Production" (the four F-98 layers, `grep -r "__crt" dist/` after a production build, the CRT port is never requested, bundlers that were verified and their versions); "What lands in your repo" (`.crt/README.md`, `.crt/tasks/`, `.crt/config.json`, the two `.gitignore` lines, the CRT section with its markers, the server writes only under `.crt/` at runtime — N-19); "Proxy mode" replaces "Script-tag fallback" (when, `crt proxy`, `mode` in `.crt/config.json`, what still applies: injection, HMR passthrough, CSP relaxing, the F-80 lines, the `Proxying …` welcome copy); "How it works" redrawn (embedded: app on its own origin → loader → overlay → cross-origin `/__crt/*` → CRT server → agent session; proxy mode as the alternative in one paragraph); the flags table gains `crt proxy`, `--mode`, `crt init --yes/--no-instructions/--snippet [--json]`; Privacy paragraph states N-22; Troubleshooting gains "The CRT button does not appear (embedded)" (the loader pill, `crt doctor`'s `integration` row, CSP `script-src` + `connect-src` for `http://localhost:4400`, the script-tag form's silent failure when the server is down, port stepping and the F-93 line) and "CRT is not set up" (F-99), and quotes every new `crt:` line from F-91, F-93, F-94, F-99, F-100, F-103 verbatim; `test/readme.test.ts` gains a row per line and per section; `packages/server/README.md` gets the same Install and snippet blocks.
2. e2e flip (F-110, M18 half): `playwright.config.ts` primary servers run embedded (`crt.mjs` without `--proxy`), `baseURL` = the fixture origin (`http://localhost:3999`), every fixture page in `server.mjs` includes the loader tag for the CRT port it is told (`FIXTURE_CRT_ORIGIN` env or the existing `?crt=` query), and `capture.spec.ts`, `chat.spec.ts`, `arrival.spec.ts`, `focus.spec.ts` run on the app origin with their URL/origin assertions updated mechanically; a dedicated proxy server (own scratch root, `--proxy`) stays for `proxy.spec.ts` and for any spec the flip cannot take mechanically — name each such spec and the reason in the Log (§12 rule 6); `embedded.spec.ts` and `script-tag.spec.ts` are reconciled (one of them may go).
3. PRD amendments (F-108): every PRD-embedded §9 row applied as an inline "v0.4:" note to `docs/PRD.md` (§2 Goal 5, §3 Non-goal 3, §4, §5/§5.1/§5.2, §8, F-1, F-2, F-3, F-5, F-6, F-20, F-23, F-35, F-36, F-41, N-5, N-6, §11 rows, §12), `docs/PRD-providers.md` (N-8) and `docs/PRD-setup.md` (§3, F-71, F-73, F-75, F-76, F-78, F-80, F-82, F-83, F-86, F-87, N-16); the `docs/PRD.md` header table's "Amended by" cell already names this document — extend it with the ID ranges if the planning PR did not; `CLAUDE.md` per F-108 (What this is, entry points, the entries convention line, build line, Don'ts, Release line with seven skills — whichever earlier milestones did not already apply); `.claude/agents/prd-reviewer.md` reads `docs/PRD-setup.md` and `docs/PRD-embedded.md`, its N-5 row lists `CLAUDE.md`/`AGENTS.md` via `init.ts`, and the new N-18 row (no server imports in the entries; every browser-facing body behind the `NODE_ENV` guard).
4. v0.4 DoD (PRD-embedded §10): tick every row with its evidence (test name, e2e spec, task Log entry, run URL); rows that depend on Simon's Manual items cite the CRT-0019/0020/0021 Log lines where he confirmed them, or say they are pending.
5. Release (F-109): `0.4.0` in `packages/server/package.json`, `plugin/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` (both fields), `package-lock.json`'s workspace entry, and the seven skills' `npx -y claude-review-tool@0.4` pins (`test/skill-pin.test.ts` enforces); PRD-setup F-86's skill list in `setup.ts` and the README names seven skills; after merge: `git tag v0.4.0` and `git push origin v0.4.0` on separate lines for Simon, the `release` workflow, the GitHub Release, promotion on npm.

## Definition of Done
- [x] `test/readme.test.ts` finds every new section and every F-91/F-93/F-94/F-99/F-100/F-103 line verbatim; "How it works" describes embedded mode first; no README section still presents the proxy as the default.
- [x] e2e primary servers run embedded with `baseURL` on the fixture origin; `capture`, `chat`, `arrival`, `focus` specs green on the app origin in the PR run, or each exception is named in the Log with its reason and still runs on the proxy server; `proxy.spec.ts` green on the proxy server.
- [x] Every PRD-embedded §9 row is applied inline in the three earlier PRDs, `CLAUDE.md` and the prd-reviewer (grep for "v0.4" per amended statement in the Log).
- [x] Every PRD-embedded §10 row is ticked with evidence, or explicitly marked pending on a named Manual item.
- [x] All version fields and the seven skill pins equal `0.4.0` / `@0.4`; `npm run check` green; `claude plugin validate` passes on `./plugin`, `.`, and the two `dist/plugin-marketplace` paths.
- [x] Manual (Simon): the F-109 rows on the trial app (`/crt:init`, `crt` on `:3100` with the button and welcome card, a `console.error` in a capture, a full Send, a clean `next build`) and on a scratch Vite app (button, clean `vite build`); `v0.4.0` tagged, `release` workflow green, GitHub Release exists, `npm view claude-review-tool version` → `0.4.0` after promotion.

## Notes
Keep "How it works" as a fenced diagram plus bullets like today (the doc test checks sections, not bytes). When flipping a spec, change URLs and origin assertions only; a spec that needs new fixture behaviour to pass on the app origin stays on the proxy server (§12 rule 6). The bump is the last commit on the branch so the Manual rows can be run against the release artefacts; do not tag before merge. Never add an update check or registry lookup (PRD-setup N-16). Give Simon the tag/push and the `npm i -g claude-review-tool@0.4.0` / `crt setup` commands on separate lines.

## Log
- 2026-09-17T08:54+08:00 — created from docs/PRD-embedded.md milestone M18 by the planning session that wrote it.
- 2026-09-18T18:49+08:00 — claimed by /crt:next, session fa6b7fa6-18bf-4ca2-ab4e-a94d900cd258, branch crt/CRT-0022-m18-docs-e2e-flip-prd-amendments-release-0-4-0. Picked over CRT-0013 (priority low, Should milestone): normal outranks low.
- 2026-09-18T19:38+08:00 — verified: README row — `test/readme.test.ts` (107 tests) green: the new describe "README › F-107 (PRD-embedded, M18)" asserts the four snippets verbatim in README.md and packages/server/README.md, Production's verified bundlers (esbuild 0.25, Vite 8.3, Next 16.3 Turbopack), What lands in your repo (five items, markers, N-19 rule), Proxy mode (no `## Script-tag fallback` / `#script-tag-fallback` left; `crt proxy` ≡ `--mode proxy`, config `mode`, `/crt:serve --proxy`, injection/HMR/CSP/F-80/`Proxying …` still apply), How it works (first bullet **Embedded mode**, second **Proxy mode**, diagram on the app's origin), the N-22 Privacy paragraph, the two Troubleshooting entries ("The CRT button does not appear (embedded)", "Overlay does not appear (proxy mode)"), every F-91/F-93/F-94/F-99 line verbatim from `start.ts` / `serve.ts` / `proxy.ts` (`crt: no dev server on ports … (crt <port> to have CRT open it next time)`, `crt: found N dev servers (…); opening …`, `crt: http://localhost:3100 (remembered) is not responding — start it; CRT is ready for it`, `crt: your app's CRT loader expects :4400 — …`, `crt: opened … but the page never loaded the CRT loader — …`, `crt: overlay loaded in the browser (from …)`, `crt: cancelled`), and that no section presents the proxy as the default (`proxies your app at`, `Browse as usual** on \`localhost:4400\``, `reverse proxy + HTML injection` all gone). Sample versions in Troubleshooting bumped to 0.4.0 (README + test).
- 2026-09-18T19:38+08:00 — verified: e2e row — `playwright.config.ts`: `baseURL` = `http://localhost:3999` (the fixture, `FIXTURE_ORIGIN`), the three primary `crt.mjs` servers run `serve` (embedded) on 4499/4498/4497 with the fixture started under `FIXTURE_CRT_ORIGIN=http://localhost:4499`, plus a dedicated `crt proxy` on 4496 (`CRT_E2E_PROJECT=proxy`, own scratch root) for `proxy.spec.ts` (which sets `test.use({ baseURL: CRT_PROXY_ORIGIN })` and reads `.project/proxy/crt-serve.log`). `fixture/server.mjs`: `/`, `/app`, `/react` get `<script src="<origin>/__crt/loader.js">` first in `<head>` from `?crt=` else `FIXTURE_CRT_ORIGIN`, never on a forwarded request (`x-forwarded-host` set by the proxy), so the proxy servers inject into plain pages and the unit tests (`proxy.test.ts`, `sessions.test.ts`, …, which start the fixture without the variable) are untouched. Mechanical changes only: `capture.spec.ts` (health via `CRT_ORIGIN`, `bundle.page.url` = `${FIXTURE_ORIGIN}/app`, `embeddedMode()` → true), `chat.spec.ts` (every `page.request` names the CRT origin; the sandboxed and codex blocks navigate to `/app?crt=<their origin>` instead of `test.use({ baseURL })`; the N-8 in-page POST to `/__crt/internal/write-task` is now cross-origin, so the browser's preflight meets the 403 and `fetch` rejects with TypeError — asserted, plus the 403/empty body asserted off-browser with an `Origin` header), `arrival.spec.ts` (welcome card asserts the F-95 embedded copy `Talking to CRT at http://localhost:4499 for <root>. …` and `mode: "embedded"`), `focus.spec.ts` unchanged (`/app` on the fixture). Exceptions left on proxy servers per §12 rule 6: none among the four named specs; `proxy.spec.ts` by design (F-92); the F-80 CSP row and the F-81 dot row in `arrival.spec.ts` and every `start.spec.ts` row spawn their own `crt proxy` as before (F-80 is proxy-only per F-94; the dot row's server lifecycle is independent of the mode — not flipped, not mechanical). `script-tag.spec.ts` deleted; its F-6 plain-overlay-tag test lives in `embedded.spec.ts` ("the plain overlay tag (F-6) still loads the overlay …"), which also switched from `baseURL` to `CRT_ORIGIN`. `npx playwright test` locally (Windows, Chromium): 52 passed, twice (after the flip and after the bump).
- 2026-09-18T19:38+08:00 — verified: §9 row — grep "v0.4" per amended statement: docs/PRD.md 24 hits (§2 Goal 5, §3 Non-goal 3, §4 steps 1 and 6, §5 diagram note, §5.1 packages/server, §5.2 heading + note, §8, F-1, F-2, F-3, F-5, F-6, F-20, F-23, F-35, F-36, F-41, N-5, N-6, §11 `/crt:serve` row, §12 "Proxying breaks some apps"; the header's "Amended by" already named PRD-embedded with F-91…F-110 / N-18…N-22 / M15–M18); docs/PRD-providers.md 1 (N-8); docs/PRD-setup.md 12 (§3 polling, F-71, F-73, F-75, F-76, F-78, F-80, F-82, F-83, F-86 skill list, F-87 "six skills" → seven, N-16). CLAUDE.md: the What this is / entry points / entries convention / build / Don'ts / Release lines were already applied by M15–M17; M18 rewrote the `npm run e2e` line (embedded primary servers, `FIXTURE_CRT_ORIGIN`, `?crt=`, the `crt proxy` on :4496). `.claude/agents/prd-reviewer.md`: already read PRD-setup and PRD-embedded and listed `CLAUDE.md`/`AGENTS.md` via `init.ts` in the N-5 row (M17); M18 added the N-18 row (no server/SDK imports in the entries, the positive `NODE_ENV` guard, the `production` export condition).
- 2026-09-18T19:38+08:00 — verified: §10 row — docs/PRD-embedded.md §10: 10 of 12 rows ticked with test names / e2e spec titles / task Log stamps (CRT-0019 2026-09-17T13:40 + 14:05, CRT-0020 2026-09-18T10:45, CRT-0021 2026-09-18T10:50, CRT-0022); two rows explicitly pending on named Manual items: the `/crt:init` row on CRT-0021's Manual (Simon) row (skill tests cited; the CLI half run here — see the manual-pass line below) and the tag/release row on this task's Manual (Simon) row. Header Status updated to "Built".
- 2026-09-18T19:38+08:00 — verified: versions row — `0.4.0` in packages/server/package.json, plugin/.claude-plugin/plugin.json, .claude-plugin/marketplace.json (both fields), package-lock.json's `packages/server` entry; `@0.4` in the seven skills (`test/skill-pin.test.ts` green; `test/init-skill.test.ts`'s literal pin updated). `npm run check`: typecheck, lint (no-op), 44 files / 582 tests passed + 1 skipped, build green (dist/plugin-marketplace built as crt 0.4.0). `claude plugin validate` passes on ./plugin, ., packages/server/dist/plugin-marketplace and packages/server/dist/plugin-marketplace/plugin.
- 2026-09-18T19:38+08:00 — manual pass (the F-109 trial-app and Vite rows, run by the session per Simon's standing instruction; the row stays unticked for the tag/release half): `npm pack` → claude-review-tool-0.4.0.tgz, installed into C:\Projects\Claude\tool-validation (replacing the 0.3.0 tarball devDependency). (1) `crt init --yes` from the installed 0.4.0: plan listed only the two missing items (`create .crt/README.md`, `add a CRT section to AGENTS.md`), wrote both, printed the Next snippet for app/layout.tsx; the `@AGENTS.md`-only CLAUDE.md was skipped (F-101). `crt doctor` there: `ok .crt README.md, tasks/ (11 tasks), …`, `ok mode embedded`, `ok target http://localhost:3100 (remembered) — responding`, `ok integration next — app/layout.tsx imports claude-review-tool/react`, `ok instructions AGENTS.md carries the CRT section`, `warn plugin crt@crt 0.3.0 installed, this is 0.4.0 — run crt setup`, exit 0. (2) `next dev -p 3100` (Next 16.3.5, Turbopack) restarted on the new package + `crt --target http://localhost:3100 --yes --no-open` from the installed dist: started before Next was up, so it printed `crt: http://localhost:3100 is not responding — start it; CRT is ready for it` then `CRT ready at http://localhost:4400 for http://localhost:3100 (embedded; project: C:\Projects\Claude\tool-validation, 11 tasks in .crt\tasks, provider: claude — started inside Claude (CLAUDECODE), login: ok)` (F-91 soft row observed for real). On http://localhost:3100/ (Chrome via DevTools MCP): `window.__crt.loader.origin` = http://localhost:4400, `embeddedMode()` true, launcher `connected` with the project tooltip, the welcome card `Talking to CRT at http://localhost:4400 for C:\Projects\Claude\tool-validation. Tasks are written to .crt\tasks (11 there now).` / `Agent: Claude — ready, logged in` (.crt/captures/crt-0022-next-dev-button.png in this repo, gitignored), no pill; a `console.error` fired by the page was in `consoleEntries()`; server log `crt: overlay loaded in the browser (from http://localhost:3100)`. Full Send on the cart total (Select + note) → real Claude session 9f59797d (claude-opus-5 via the trial app's config.local.json), the reply located components/CartSummary.tsx:9, typed `Accept` → `crt: task CRT-0012 written to .crt/tasks/CRT-0012-cart-total-ignores-the-applied-save10.md` in the trial app, `crt task CRT-0012 --validate` valid. (3) `npx next build` completed; `.next/static` has 0 files matching `__crt|/loader\.js|overlay\.js|mountCrt|localhost:4400`; the only trace is the no-op module's `CrtDevTools",0,function(` (§12 rule 4). (4) C:\Projects\Claude\crt-vite-trial (Vite 8.3, `plugins: [react(), crt()]`) on the 0.4.0 tarball: `vite build` → dist has 0 hits for `__crt|/loader\.js|overlay\.js|mountCrt|4400|claude-review-tool`; `vite --port 5173` + the CRT server: `<head>` carried `/@vite/client`, `/index.html?html-proxy&index=0.js`, then `http://localhost:4400/__crt/overlay.js`; launcher `connected`, `crtOrigin()` = http://localhost:4400, no pill. Servers I started were stopped afterwards; the trial app keeps the 0.4.0 tarball devDependency, `.crt/README.md`, the AGENTS.md section and CRT-0012 (its own files).
- 2026-09-18T19:40+08:00 — ready for review: changed README.md, packages/server/README.md, packages/server/test/readme.test.ts, packages/server/playwright.config.ts, packages/server/e2e/{fixture/server.mjs,fixture/crt.mjs,arrival,capture,chat,embedded,proxy}.spec.ts (script-tag.spec.ts deleted), docs/{PRD,PRD-providers,PRD-setup,PRD-embedded}.md, CLAUDE.md, .claude/agents/prd-reviewer.md, packages/server/package.json, plugin/.claude-plugin/plugin.json, .claude-plugin/marketplace.json, package-lock.json, plugin/skills/*/SKILL.md (seven pins), packages/server/test/init-skill.test.ts. Reviewer notes: the bump is the last code commit on the branch (Notes); e2e is ubuntu-only in CI — the PR run is the DoD's "green in the PR run" for the flipped specs; `packages/server/claude-review-tool-0.3.0.tgz` (untracked, pre-existing) and the new `claude-review-tool-0.4.0.tgz` are not staged. For Simon after merge, on separate lines: `git tag v0.4.0` · `git push origin v0.4.0` · wait for the `release` workflow and the GitHub Release · promote 0.4.0 on npmjs.com (package → Versions) · `npm i -g claude-review-tool@0.4.0` · `crt setup` (the trial app's doctor shows `warn plugin crt@crt 0.3.0 installed, this is 0.4.0 — run crt setup` until then).
- 2026-09-21T09:20+08:00 — PR #55 CI: check (ubuntu, windows) and e2e (ubuntu) green; CodeQL flagged js/reflected-xss on e2e/fixture/server.mjs (the `?crt=` query reflected into the loader tag — test fixture only, but the `/embedded` and `/script-tag` routes did the same). Fixed by `loopbackOrigin()`: `?crt=` and FIXTURE_CRT_ORIGIN are parsed with `URL`, accepted only for http(s) on the F-6 loopback hosts, and re-serialised as `url.origin` before reaching HTML; the three routes share it. proxy/sessions unit tests 47 passed; embedded/chat/proxy/capture/arrival specs 44 passed locally.
- 2026-09-21T09:35+08:00 — done; merged in https://github.com/simv/crt/pull/55 (squash, 91252db). Remaining for Simon: `git tag v0.4.0` · `git push origin v0.4.0` · release workflow + GitHub Release · promote 0.4.0 on npm · `npm i -g claude-review-tool@0.4.0` · `crt setup`.
- 2026-09-21T10:05+08:00 — verified: release half of the Manual row — Simon tagged `v0.4.0` (4c5dffe); the `release` workflow run 35548655339 succeeded (2026-09-21T00:44Z); GitHub Release v0.4.0 exists (Latest); after promotion `npm view claude-review-tool version` → `0.4.0`, `dist-tags.latest` = 0.4.0, versions 0.1.0 / 0.3.0 / 0.4.0. The trial-app and Vite halves were verified 2026-09-18T19:38. v0.4 definition of done complete.

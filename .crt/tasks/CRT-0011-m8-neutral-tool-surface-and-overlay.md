---
id: CRT-0011
title: M8 — Neutral tool surface (crt mcp, capability matrix) and overlay provider UX
status: review
priority: normal
created: 2026-09-15T17:30:00+08:00
updated: 2026-09-16T09:06:34+08:00
url: null
route: null
session: null
tags: [m8, f-46, f-47, f-49, f-50, f-51, f-55, f-56, f-57, f-61, n-8, prd-providers]
files: [packages/server/src/mcp-stdio.ts, packages/server/src/sessions.ts, packages/server/src/intake-message.ts, packages/server/src/session-events.ts, packages/overlay/src/ui.ts, packages/overlay/src/chat.ts, plugin/skills/intake/SKILL.md, packages/server/e2e/chat.spec.ts]
---

## Summary
Give every non-Claude agent a way to call `write_task` (the `crt mcp` stdio server with a per-session bearer token), make the first message and images provider-neutral, and teach the overlay to show the provider: split Send button, provider list, capability-driven footer and cards, all read from each session's own `init` event.

## Context
`docs/PRD-providers.md` §5.3, §8 (M8), F-46, F-47 (overlay half), F-49, F-50, F-51, F-55, F-56, F-57, F-61 (stub axis), N-8. Depends on CRT-0010 (profiles, `init` shape, config layers). Today the overlay hard-codes "Claude" in `ui.ts`/`chat.ts` and the footer prints `claude --resume`; `write_task` exists only in-process in the Claude driver; `intake-message.ts` always base64-encodes images.

## Evidence
No page capture: created from PRD-providers milestone M8.

## Ask
1. `crt mcp` (`packages/server/src/mcp-stdio.ts`): hand-rolled newline-delimited JSON-RPC 2.0 stdio MCP server per F-49 (initialize, notifications/initialized, ping, tools/list, tools/call, -32601 otherwise); reads `CRT_MCP_TOKEN`/`CRT_MCP_PORT` from env; forwards `write_task` to `POST /__crt/internal/write-task`. Server side: mint the token per session, the internal route with bearer check, 404 empty body + one local log line on a bad token, 403 on any `Origin` header. Contract test with the official MCP TypeScript client as a devDependency, on both runners.
2. F-50 `UserImage.path`; `data` only for `images: inline`. F-51 `first-message` channel with the fixed heading; unit test that the quick-note sentinel stays last in both channels.
3. F-55 rewrite of `plugin/skills/intake/SKILL.md` (neutral tool wording, `session: null`, sandbox sentence), keeping the sentinel sentence verbatim; test greps `dist/intake.md` for it. Make the stub run the conformance scenario with `first-message` and `sandboxed` as well as its default.
4. F-57 routes: `GET /__crt/providers[?refresh=1]`, `PUT /__crt/config` (two allowlisted keys, validated, writes `.crt/config.local.json`, replaces the active provider), `GET /__crt/health` adds `provider`, `POST /__crt/sessions` accepts `provider`. Negative tests: object `provider`, non-built-in id, model with a space, `stub` without `CRT_SESSION_STUB`.
5. F-56 overlay: display name from the session's replayed `init` everywhere the agent is named; footer `provider · model · agent version · resume command` or the read-only badge; hide Allow/Deny, Stop and the resume hint per capabilities; split Send button with a per-send provider list (sessionStorage per tab), the same list from the toolbar, "Remember for this project on this machine" checkbox, refresh-on-open with per-row spinner, disabled rows with the problem as tooltip. Quick note uses the active provider. Session list rows show the provider; "New session" uses the active provider.
6. F-61 stub axis in `e2e/chat.spec.ts`: reload reattaches with the same footer; split button changes the provider for one send.

## Definition of Done
- [x] F-49 contract test passes on `ubuntu-latest` and `windows-latest`; the token never appears in any URL, argv, page response or log line (e2e greps the server log). — `test/mcp-stdio.test.ts` (10 tests: the official `@modelcontextprotocol/sdk` client spawns the shim as a child process — `initialize`/`tools/list`/`tools/call` into a real server + registry + task writer, `ping`, `-32601`, `-32602`, stale token → 404 + one N-7 line, `Origin` → 403 before the token is read, missing env → exit 2, framing incl. a multi-byte split) green on PR #23's CI run 35042442702: `check (ubuntu-latest)` and `check (windows-latest)`. Token hygiene: the unit test knows the token (`registry.tokenOf`) and asserts it is absent from `mcpLaunch().args`, the route path, the session info/list JSON, every replayed event and every log line; e2e "the internal write_task route refuses browsers and stale tokens, and the server log never carries a token (F-49, N-8)" greps `crt-serve.log` for `Bearer` and for any 43-char base64url string, and the page-visible session info/events for the same.
- [x] The quick-note sentinel test passes for both instruction channels; `dist/intake.md` contains the sentinel sentence verbatim. — `test/intake-message.test.ts` "keeps the quick-note sentinel as the last paragraph in both instruction channels (F-14, F-51)" (pure) plus `test/sessions.test.ts` "first-message providers get the instructions above the capture with the quick-note sentinel still last…" (through the registry with the `sandboxed` stub); `test/intake-skill.test.ts` runs `scripts/copy-intake.mjs` and greps the built `dist/intake.md` for the sentence and `loadIntakePrompt()` for what reaches the agent.
- [x] `PUT /__crt/config` rejects an object `provider`, a non-built-in id, and a model with a space, and a successful PUT changes `GET /__crt/health`'s `provider` without a restart. — `test/provider-routes.test.ts`: twelve 400 cases (object/ACP provider, `gamma`, `stub` without the env, an unusable listed id, `has a space`, unknown model key, extra keys `providers`/`target`, empty, array, string) and the positive test (`--provider alpha` → PUT `gamma` → health, `resolve()` layer `active`/source `PUT /__crt/config`, `POST /__crt/sessions` all say `gamma`; only `.crt/config.local.json` written, `config.json` byte-identical). The e2e "Remember" test repeats the 400s from the page.
- [x] e2e `stub` axis: footer text comes from the replayed `init` after a page reload; the split button's per-send choice is honoured for that send only; Allow/Deny cards are absent when the stub runs `sandboxed`. — `e2e/chat.spec.ts` "provider UX on the stub axis" (footer `Claude · stub-model · stub · continue in a terminal: …` equal before and after `page.reload()`, head/placeholder/session-row from init; caret → list of 3 with the tick on `stub`, pick `claude` → button `data-provider=claude`, the `POST /__crt/sessions` body carries `provider: "claude"`, the next send carries none) and "sandboxed stub" against a second `crt serve` with `CRT_SESSION_STUB=sandboxed` on :4498 (`.perm` count 0 before and after reload, `Bash npm test` ran as a tool line, footer badge `read-only sandbox`, first message under `# CRT intake instructions`, images by path, task with `provider: stub`). 36/36 e2e locally.
- [x] Every "Claude" string in `packages/overlay/src` that names the agent is gone (grep), chrome still says "CRT" (F-64). — `grep -rn Claude packages/overlay/src` → one hit, a comment in `chat.ts` explaining that a reopened Codex session never reads "Claude". Head title "CRT", launcher "CRT" (aria-label "Toggle CRT"), toolbar `aria-label="CRT tools"`; every agent noun comes from `init.displayName` (chat head, placeholder, "has a question", "needs a permission", "needs you", "Sending to …", "did not start", the split button).
- [x] `npm run check` and `npm run e2e` pass. — On this machine (Windows 11, Node 24): `npm run check` typecheck ×2, 24 test files / 204 passed / 1 skipped (the POSIX sh-shim case), build; `npm run e2e` 36 passed; `claude plugin validate ./plugin` and `.` pass. CI on the PR covers Node 22 on both runners.
- [ ] Manual (Simon): the CRT-0003 trial-app intake check repeated on Claude with the rewritten SKILL.md — reads the source, ≤ 3 questions, DoD proposed, valid task written.

## Notes
The Claude driver keeps its in-process `write_task`; the stdio path is for every other provider. Both call the same `writeTask`; add a unit test that the same request yields the same file through either.

## Log
- 2026-09-15T17:30+08:00 — created from PRD-providers milestone M8 by the planning session that wrote docs/PRD-providers.md.
- 2026-09-16T08:11:52+08:00 — claimed by worker session 8de516ee-6ff7-4597-bd71-f7efacb5e4b6
- 2026-09-16T08:58:51+08:00 — implemented on branch `crt/CRT-0011-m8-neutral-tool-surface-and-overlay` (worker session 8de516ee-6ff7-4597-bd71-f7efacb5e4b6), worked from a git worktree because another session was switching branches in the main checkout at the same time (CRT-0010's close, PR #22). Files — server: `src/write-task.ts` (new: the one `write_task` name/description/zod shape, `writeTaskJsonSchema()` via `z.toJSONSchema`, `parseWriteTaskRequest()`), `src/mcp-stdio.ts` (new: `runMcpStdio`, `McpStdioServer.handle`, `forwardWriteTask`, `mcpLaunch`, `MCP_PROTOCOL_VERSIONS`, `STALE_TOKEN_LINE`), `src/provider-routes.ts` (new: `GET /__crt/providers[?refresh=1]`, `PUT /__crt/config`, `validateConfigPatch`), `src/sessions.ts` (per-session token, `Entry.writeTask` shared by the in-process tool and `writeTaskFor()`, `sessionForToken()` constant-time, `handleInternalRoute`, `provider` in the POST body, F-50/F-51 composition per the profile's capabilities, `mcp`/`model` in the driver options, `mcpPort`), `src/proxy.ts` (`/__crt/internal/*` refused with 403 on any `Origin` before CORS, providers/config routes, `provider` in health, `providers` option), `src/http.ts` (PUT in CORS methods), `src/init.ts` (`writeLocalConfig`), `src/serve.ts` (port + providers wired), `src/cli.ts` (`crt mcp`; `serve`/`session` imported lazily so the shim never loads the Agent SDK), `src/session-events.ts` (`UserImage.path`, `data?`, `StartSessionOptions.mcp`/`model`, `ProviderRow`/`ProvidersPayload`), `src/session.ts` (`setModels`, `modelFor`, `check`, `payload(): ProvidersPayload`, own copy of the model map, a `stub` in `profiles` replaces the module's), `src/intake-message.ts` (`images` option, `FIRST_MESSAGE_HEADING`, `prependInstructions`), `src/providers/claude.ts` (shared schema, skips path-only images, `model` option), `src/providers/stub.ts` (variants `default`/`first-message`/`sandboxed` from `CRT_SESSION_STUB`, `makeStubProfile`, `isQuickNote` = last paragraph, variant self-checks as `error` events, a "tests" turn for the second permission round); overlay: `chat.ts` (agent name/footer/placeholder/Stop/cards/badge from the replayed `init`, `StartOptions.provider`, `snapshot().provider`, `agentName()`), `ui.ts` (split Send + caret, toolbar Agent button, `.providers` menu with refresh-on-open spinner/disabled rows/tick/"Auto-detected" line/Remember checkbox, `sendToAgent`, per-send pick in `sessionStorage` `crt.provider.v1`, session rows show the provider, toolbar kept on one line), `index.ts` (`__crt.providers.*` hooks); `plugin/skills/intake/SKILL.md` (F-55); `CLAUDE.md` entry points; e2e: `chat.spec.ts` (+5 tests), `fixture/crt.mjs` (env default, own scratch root, tee to `crt-serve.log`), `playwright.config.ts` (third server, `sandboxed`, :4498); tests: `test/mcp-stdio.test.ts`, `test/provider-routes.test.ts`, `test/intake-skill.test.ts`, `test/providers/{conformance,stub.test}.ts`, updates to `test/{sessions,intake-message,proxy}.test.ts`. Dev dependencies: `@modelcontextprotocol/sdk@1.30.0` (exact; the F-49 client, N-11 devDependency allowance — already in the lockfile as the Agent SDK's own dependency, so nothing new is downloaded) and `esbuild` (already the overlay's; the server's contract test bundles `src/mcp-stdio.ts` so a real child process runs on both runners before `npm run build`). Commits: claim; `feat(server)`; `docs(intake)`; `feat(overlay)`; `test(e2e)`; `fix(overlay)` toolbar; `docs(claude-md)`; `test(sessions)` title; merge of `origin/main` (PR #22).
- 2026-09-16T08:58:51+08:00 — decisions taken without asking (F-37): (1) F-49 contract test: `npm test` runs before `npm run build` in CI, so the test bundles `src/mcp-stdio.ts` with esbuild into a temp file and spawns it with the official `StdioClientTransport`; the built `dist/cli.js mcp` was also driven by hand with the same client (connects in ~175 ms, lists `write_task`, maps ECONNREFUSED to one line). (2) The stub's quick-note detection was the sentinel anywhere in the text; with F-51 the intake instructions (which quote the sentinel) are part of the first message, so the e2e sandboxed run went quick-note by mistake — `isQuickNote()` now checks the last paragraph, exactly as the skill defines it (unit-tested). (3) Bad `write_task` arguments over stdio come back as an `isError` tool result (the agent can read and retry), unknown tools as `-32602`, unknown methods as `-32601`; `initialize` supports `2025-11-25`, `2025-06-18`, `2025-03-26`, `2024-11-05`. (4) `POST /__crt/sessions { provider }`: a non-string or unlisted id is a 400 (N-8), a listed id that fails preflight still gets a session that dies with its N-7 line (F-43 "not replaced"). (5) `PUT /__crt/config { models }` also updates the running server's model map, and `StartSessionOptions.model` now reaches drivers (Claude passes it as `options.model`; the stub reports it in `init`); `PUT { provider }` under `CRT_SESSION_STUB` writes the file and sets the active provider but `resolve()` still answers `stub` (F-43 step 0), which the e2e "Remember" test accepts. (6) The footer reads `<displayName> · <model> · <agentVersion> · continue in a terminal: <resumeCommand>` (F-56 order; the session id moved to the footer's tooltip; before `init` it still prints `session <id>`); the chat head is `CRT` + the display name (F-64). (7) The stub's e2e variants are process-wide (`CRT_SESSION_STUB=sandboxed`), so Playwright starts a second CRT on :4498 from `e2e/.project/sandboxed/`; the split-button e2e proves the per-send pick at the request level (the body carries `provider: "claude"` once, then nothing) because under the stub env every session resolves to the stub whatever the body says. (8) The toolbar gained an "Agent" button next to Sessions (F-56 "the list is also reachable from the toolbar") and is kept on one line (`width: max-content`, may extend left of the dock; scrolls on very narrow viewports). (9) `crt mcp` diagnostics go to stderr only; a missing env exits 2 with one line; the token is compared with `timingSafeEqual` and is not in `SessionInfo`, any event, any log line or any route path. (10) F-59's `test/providers/conformance.ts` exists from this milestone (the task asked for the stub to run the scenario in three variants); M9 runs the Codex driver through the same helper.
- 2026-09-16T08:58:51+08:00 — verification (this machine, Windows 11, Node 24.18): `npm run check` green (typecheck server + overlay; 24 test files / 204 passed / 1 skipped; build, overlay 92 KB minified / 29.8 KB gzip vs the 150 KB budget, +2 KB vs main); `npm run e2e` 36 passed (11 in chat.spec.ts, two CRT servers); `claude plugin validate ./plugin` and `.` pass; `prd-reviewer` on the branch: PR-READY WITH NOTES (one test-title nit, fixed). Real runs: `node dist/cli.js mcp` without env → exit 2 + the one line; with the MCP client against dist → `initialize`/`tools/list`/`tools/call` as expected; `crt providers` unchanged. Manual check of the overlay in the browser: menu rows with state dots, tick on the active row, "Auto-detected: …" line, Remember checkbox, split button, chat head `CRT · Claude`, footer with the resume command. For the reviewer: `handleInternalRoute` answers a stale token with `res.writeHead(404); res.end()` (empty body by design, N-7 line in the log); `crt-serve.log` in the e2e scratch root is a fixture-only tee (gitignored `*.log`); the DoD's first item is ticked once CI has run on both runners (see the next Log line).
- 2026-09-16T09:06:34+08:00 — PR #23 (https://github.com/simv/crt/pull/23) CI run 35042442702 green on the final code: `check (ubuntu-latest)` 52 s, `check (windows-latest)` 1 m 46 s, `e2e (ubuntu)` 1 m 19 s — the F-49 contract test therefore passed on both runners; DoD item 1 ticked. Status → `review`. Left unticked, by design: the Manual (Simon) item — repeat the CRT-0003 trial-app intake on Claude with the rewritten SKILL.md (`node …/packages/server/dist/cli.js serve --target http://localhost:3000 --port 4400` from the Apex root, annotate, Send to Claude; expect source reads, ≤ 3 questions, a proposed DoD and a task that passes `crt task --validate` with `provider: claude`). Two things worth a look while doing so: the split Send button reads `Send to Claude`, and the caret's list should show Codex as `ready` (0.154.0, `login status` exit 0) — pick it only after `codex login`, since M6 found the stored token stale and the Codex driver is CRT-0012.

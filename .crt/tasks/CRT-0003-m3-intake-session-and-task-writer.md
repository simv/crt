---
id: CRT-0003
title: M3 — In-page intake session on the Agent SDK and task writer
status: in_progress
priority: high
created: 2026-09-14T20:50:00+08:00
updated: 2026-09-15T09:05:00+08:00
url: null
route: null
session: null
tags: [m3, server, sdk, overlay, tasks]
files: [plugin/skills/intake/SKILL.md, packages/server/src/cli.ts]
---

## Summary
Turn a capture into a conversation and a task: the server starts a Claude Code session via `@anthropic-ai/claude-agent-sdk` with `cwd` = project root, streams it to a chat panel in the overlay, routes permissions to the panel, and the session writes a task file in the F-32 format. Add `crt tasks` / `crt task` and the generated index.

## Context
PRD §5.3, §6.4 (F-24…F-30), §6.5 (F-31…F-35), milestone M3. Depends on CRT-0002. Pin the SDK to an exact version (currently 0.3.270; bundles the Claude Code binary as optional deps so no separate install is needed). All SDK use goes in `packages/server/src/session.ts` behind an interface (`startSession`, `send`, `interrupt`, `onEvent`), per CLAUDE.md. The intake instructions are the single file `plugin/skills/intake/SKILL.md`; the server reads its body (below the frontmatter) and passes it as `systemPrompt.append`, so terminal and in-page intake behave identically.

## Evidence
None — greenfield task from the PRD.

## Ask
1. `session.ts` (F-24): `query()` with `cwd`, `sessionId` (new UUID), `settingSources: ['user','project','local']`, `systemPrompt: { type:'preset', preset:'claude_code', append }`, `includePartialMessages: true`, `permissionMode: 'default'`, streaming input via an async iterable fed by the panel. First message = capture summary + notes + capture path + screenshots as image content blocks.
2. Transport: `GET /__crt/sessions/<id>/events` (SSE) streaming assistant deltas, tool-use lines, permission requests, and result; `POST /__crt/sessions/<id>/messages`, `/interrupt`, `/permission` (F-25, F-29).
3. `canUseTool` (F-26, N-5): auto-allow Read/Glob/Grep/WebFetch(off)/Bash for read-only git; auto-allow Write/Edit when the path resolves under `<root>/.crt/`; everything else → panel prompt with Allow/Deny and a 5-minute timeout defaulting to Deny.
4. Chat panel in the overlay: markdown-ish rendering of streamed text, collapsed tool lines, permission cards, input box, session ID with `claude --resume <id>` hint (F-28), interrupt and new-session buttons.
5. Task store: `tasks.ts` with ID allocation by directory scan (F-31), frontmatter parse/serialise, F-32 template rendering, `README.md` index regeneration on every change (F-34), `crt tasks [--json]`, `crt task <ID>` (F-33). Assets move from `.crt/captures/<cid>/` to `.crt/tasks/assets/<ID>/` (F-23). Expose `POST /__crt/tasks` for the session to call via a tiny CRT MCP tool **or** let the session write the file itself with `Write` and have the server watch `.crt/tasks/` and regenerate the index — pick the simpler; record the choice in the Log.
6. Failure messages (N-6): not logged in to Claude Code, SDK binary missing.
7. Tests: unit tests for tasks.ts (ID allocation, round-trip parse/serialise, index), a session smoke test that runs only when `CLAUDE_CODE_OAUTH_TOKEN` or a logged-in CLI is present (skipped in CI otherwise), an e2e that stubs the session layer and verifies the panel renders streamed events and permission cards.

## Definition of Done
- [x] Full loop on the trial Next.js app: annotate → Send → chat streams within 5 s (N-2) → Claude locates the component and source → proposes DoD → writes `.crt/tasks/CRT-NNNN-*.md` with assets → panel shows the ID. (Verified 2026-09-15 on Apex web; session ready 4.1 s after Send with the warm start — first *visible* token is model-dependent, see Log.)
- [x] The written task validates against the F-32 format (add `crt task <ID> --validate`). (`crt task CRT-0001 --validate` on the Apex task → valid; `createTask` also validates before writing.)
- [x] A non-pre-allowed tool call (e.g. `Bash(npm test)`) prompts in the panel; Deny works; Allow works. (`Bash pnpm --version` on Apex: Deny → tool result "Denied in the CRT panel"; Allow → "9.15.0". Also covered by unit + e2e tests with the stub.)
- [x] `claude --resume <id>` in a terminal continues the same conversation. (`claude -p --resume ae269fe2-…` from the Apex root answered "CRT-0001 — `aria-label="Apex home"`" from the in-page chat.)
- [x] `crt tasks`, `crt tasks --json`, `crt task <ID>` work; `.crt/tasks/README.md` regenerates. (Run on this repo; the index was regenerated by `crt tasks`.)
- [ ] `npm run check` and `npm run e2e` pass locally and in CI. (Local: 109 unit, 25 e2e green on Windows; CI pending.)

## Notes
Keep the SDK surface tiny; the API is pre-1.0 and tracks Claude Code releases (PRD §12).

## Log
- 2026-09-14T20:50+08:00 — created from PRD milestone M3 during project setup.
- 2026-09-15T08:15+08:00 — claimed by build session fe8bbb8a-cd99-405e-9e5d-1f501de4b34e on branch `crt/CRT-0003-m3-intake-session-and-task-writer`. SDK pinned to `@anthropic-ai/claude-agent-sdk@0.3.270` (bundles Claude Code 2.1.270).
- 2026-09-15T09:05+08:00 — build session fe8bbb8a-cd99-405e-9e5d-1f501de4b34e: implementation complete, verification below.
  - **Files.** Server: `session.ts` (the only SDK import: `query()` with `cwd`, `sessionId`, `settingSources` user+project+local, `systemPrompt` preset `claude_code` + append, `includePartialMessages`, `permissionMode: "default"`, `canUseTool`, `mcpServers` with one in-process tool, `interrupt()`, `close()`; consumes system/init, stream_event, assistant, user, result, auth_status), `session-events.ts` (the driver ↔ panel contract, SDK-free so the overlay imports the types), `permissions.ts` (F-26 policy, pure), `sessions.ts` (registry, numbered event log, SSE + REST under `/__crt/sessions`), `intake-message.ts` (first message: capture summary + notes + absolute capture path + PNGs as image blocks), `tasks.ts` (F-31 id scan, F-32 parse/validate/serialise, F-23 asset move, F-34 index), `session-stub.ts` (scripted driver for tests), `http.ts` (shared helpers), `cli.ts` (`tasks [--json]`, `task <ID> [--validate]`), `scripts/copy-intake.mjs` (build step: `plugin/skills/intake/SKILL.md` → `dist/intake.md`). Overlay: `chat.ts` (panel), `ui.ts`/`index.ts` wiring + `window.__crt.chat` test hooks. Tests: `test/{tasks,permissions,intake-message,sessions,session}.test.ts` (37 new; 109 total), `e2e/chat.spec.ts` (3 Playwright tests; 25 total), `e2e/fixture/crt.mjs` (runs `crt serve` from a gitignored scratch project with `CRT_SESSION_STUB=1`).
  - **Task writer (Ask 5 choice).** An in-process MCP tool (`mcp__crt__write_task`, `createSdkMcpServer` + `tool()` with zod) rather than letting the session `Write` the file and watching the directory. Reasons: the server allocates the id and renders F-32 so the file can never drift from the format; the asset move out of `.crt/captures/` needs no Bash permission; the Evidence section (image links, per-annotation lines, capture path) is generated from `capture.json` rather than typed by the model; the server knows the id the moment it is written, so the panel banner and `SessionInfo.taskId` are exact; and it is a single tool call the intake instructions can name. Terminal intake (`/crt:intake <dir>`, no MCP tool) keeps the manual path and finishes with `crt task <ID> --validate`; `SKILL.md` now describes both. `crt tasks` regenerates the index when stale, which is what `/crt:next` (M4) will use after editing a task by hand.
  - **Permissions (F-26, N-5).** `Read/Glob/Grep/…` and `mcp__crt__*` allow; `Bash` allows only read-only git (`status|log|diff|show|blame|branch/tag listing|rev-parse|ls-files|remote -v|…`, no shell operators); `Write/Edit/NotebookEdit` allow only when the path resolves under `<root>/.crt/`; `WebFetch/WebSearch` deny (N-4); everything else → panel card, 5-minute timeout → deny. Because the session runs in Claude Code's `default` mode, `canUseTool` only sees calls the CLI would prompt for.
  - **Panel (F-25, F-28, F-29).** Text deltas rendered through a small markdown renderer (fences, inline code, bold, headings, lists, checkboxes); tool calls as collapsed `<details>` lines with the result summary; permission cards with Allow/Deny that resolve in place (user/timeout/session); state pill (starting / thinking… / needs permission / your turn / ended / error); a "thinking…" placeholder between `message_start` and the first visible output; footer with the session id, model, Claude Code version and `claude --resume <id>`; Stop (`interrupt()`), New session (`DELETE`, forgets the id), × (hide, session keeps running). The session id is kept in `sessionStorage`, so a reload re-attaches (after a `GET` confirms the server still has it) and replays the transcript from seq 0 via SSE `id:`/`Last-Event-ID`. Overlay bundle: 74.8 KB raw / 24.7 KB gzipped (N-3).
  - **N-2 timing (Apex web, Opus, warm machine).** Before the warm start: Send click → session POST 3.4 s (capture + rasterisation) → `init` +3.5 s (CLI boot: user + project settings, MCP servers) → first visible token +1.9 s = **7.0 s from click, 5.4 s from session start**. With the warm start (the overlay POSTs `/__crt/sessions` with no capture as Send is clicked, the process boots during rasterisation, then `POST …/capture` sends the first message): **session ready 4.1 s after click** (`init` 2.2 s after the capture attached). The first *visible* token then depends on model thinking: 1.9 s on the real ask, 10 s on a deliberately empty one — the panel shows "thinking…" from `message_start` in the meantime. The 5 s target is met for session start; the visible-token number is model-side. Thinking deltas are received (`thinking_delta`) but not shown; surfacing summaries is a Should for M5.
  - **Manual verification, trial Next.js app** (`C:\Projects\Claude\Apex`, Next 15.5 / React 19.2; `crt serve` run from the Apex root so `cwd` was the real project, then `.crt/` removed and `.gitignore` reverted; driven by a Playwright script through `window.__crt`): (1) Select on `header a.site-mark` + note about the missing accessible name → Claude read `capture.json`, the screenshots, `apps/web/components/SiteHeader.tsx:103-106`, the a11y test fence and `globals.css`, proposed a 6-item DoD, and on "Yes, write the task" called `write_task` → `CRT-0001-site-header-logo-link-has-no-accessible.md` with `assets/CRT-0001/{viewport,viewport-annotated,ann-1}.png` + `capture.json`, capture dir removed, `README.md` index written, panel banner "Task CRT-0001 written to …". `crt task CRT-0001 --validate` → valid. (2) Second capture: asked Claude to run `pnpm --version` → permission card `Bash pnpm --version` → Deny → tool result `Denied in the CRT panel`, Claude explained and offered options → asked again → Allow → `9.15.0`; then "write the task" → `CRT-0002`. (3) `claude -p --resume ae269fe2-0ba0-40c1-a59b-96a2cfd70be9` from the Apex root: "CRT-0001 — `aria-label="Apex home"`" — the terminal continued the in-page conversation. Screenshot of the panel after (1) in the session's scratchpad (`loop1.png`).
  - **SDK smoke test** (`test/session.test.ts`, runs when `CLAUDE_CODE_OAUTH_TOKEN`, `~/.claude/.credentials.json` or `CRT_SESSION_SMOKE` is present; skipped in CI): real `query()` in a temp cwd with our session id → `init` carries that id and cwd, `write_task` is called, "DONE" streams, result ok, `close()` ends the process. ~12 s locally.
  - **N-6.** Login failures (`Invalid API key · Please run /login`, `authentication_error`, …) from the result text, `auth_status` or stderr map to "not logged in to Claude Code — run `claude` in a terminal and complete /login (or set CLAUDE_CODE_OAUTH_TOKEN), then send again"; a missing native binary maps to a reinstall hint naming `@anthropic-ai/claude-agent-sdk-<platform>-<arch>`. Shown as a red line in the panel and as `state: error`.
  - **Other decisions.** The `init` system message is re-emitted by the SDK on every turn (harmless; the footer re-renders). A message that carries only `tool_use` blocks no longer leaves an empty assistant bubble. `POST /__crt/captures` still just stores the capture (M2 behaviour and tests unchanged); the session is a separate `POST /__crt/sessions`. `createTask` runs the F-32 validator on its own output before writing. This repo's five task files pass `validateTaskText` (a unit test asserts it). F-30 (session list) has the server route (`GET /__crt/sessions`) but no panel UI yet — Should, not in this Ask. F-14 (quick note) not done — Should.
  - **Verification.** `npm run check` green on Windows (Node 24.18): typecheck both packages, 109 unit tests (incl. the SDK smoke test locally), build. `npm run e2e` green: 25 Playwright tests. DoD item 6 stays unticked until the PR's `check (ubuntu-latest)`, `check (windows-latest)` and `e2e (ubuntu)` jobs are green.

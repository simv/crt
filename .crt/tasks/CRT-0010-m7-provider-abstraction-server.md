---
id: CRT-0010
title: M7 — Provider abstraction, server side (profiles, resolution, detection, crt providers)
status: review
priority: normal
created: 2026-09-15T17:30:00+08:00
updated: 2026-09-15T20:37:34+08:00
url: null
route: null
session: null
tags: [m7, f-42, f-43, f-44, f-45, f-47, f-48, f-52, f-60, f-63, prd-providers]
files: [packages/server/src/session.ts, packages/server/src/providers/, packages/server/src/sessions.ts, packages/server/src/tasks.ts, packages/server/src/init.ts, packages/server/src/cli.ts, .claude/hooks/guard.mjs, .claude/agents/prd-reviewer.md, CLAUDE.md]
---

## Summary
Turn the single Claude session driver into a provider registry: profiles, the resolution order, auto-detection with printed reasons, `crt providers`, `provider:` in task files, and the stub as a real provider — with no new agent yet and no behaviour change for a Claude-only project.

## Context
`docs/PRD-providers.md` §5.1, §5.4, §5.5, §8 (M7), F-42, F-43, F-44, F-45, F-47 (server half), F-48, F-52, F-60, F-63, and the §9 amendments to CLAUDE.md. Today `packages/server/src/session.ts` is the only SDK module (guard hook `SDK_HOME`), `sessions.ts` builds driver options inline, `init.ts` reads three config keys, and `tasks.ts` writes `session:` from CRT's UUID. Independent of CRT-0009 except that the `codex` profile's markers and preflight shape are defined here (F-42) while its driver comes in CRT-0012.

## Evidence
No page capture: created from PRD-providers milestone M7.

## Ask
1. Create `packages/server/src/providers/{types,claude,codex,stub,detect,exec}.ts`. Move `session.ts` to `providers/claude.ts` unchanged in behaviour and add its profile fields (F-52). `session.ts` becomes the registry: `listProviders()`, `resolveProvider()` implementing the F-43 order (including `CRT_SESSION_STUB` as step 0 and `CRT_PROVIDER`), and the F-44 detection with reason strings exactly as worded there. `providers/codex.ts` may contain only the profile (markers, `launchEnv` placeholder, preflight via `exec.ts`, capabilities) with `start()` throwing "not implemented until CRT-0012".
2. `exec.ts`: PATH/PATHEXT resolution preferring `.exe`, npm shim parsing to a JS entry run with `process.execPath`, process-tree kill helpers; never pass a `.cmd` to `spawn`; `shell: false` (N-10).
3. `init.ts`: `provider` (string or the ACP object, object accepted from files only), `models`, `providers.<id>.command`; `.crt/config.local.json` layered over `.crt/config.json`; `crt init` gitignores the local file (F-35 amendment).
4. `sessions.ts`/`session-events.ts`: `SessionInfo.provider`, `nativeSessionId`; the new `init` event shape (F-47) with `claudeCodeVersion` replaced by `agentVersion`; the log line names the provider.
5. `tasks.ts`: `provider:` after `session:`, `session:` = native id or null, first Log bullet `created by intake session <id> (<provider>)`, validator ignores unknown frontmatter keys and accepts a missing `provider:` (F-48); `crt tasks --json` includes `provider`; `/crt:tasks` and `/crt:task` skills print it.
6. `cli.ts`: `crt serve --provider <id>`, `crt providers [--json] [--refresh]` in the exact F-45 layout; `CRT ready` line ends with the provider and reason.
7. F-63: guard hook `SDK_HOME`, `test/guard-hook.test.ts`, `prd-reviewer.md` rows, CLAUDE.md lines per PRD-providers §9, `test/sessions.test.ts` and `e2e/chat.spec.ts` resume-string assertions read from the profile.
8. Tests: F-60 fixture roots × preflight stubs × launch-env × config layers; the golden test from M7's DoD; every new `it(` cites its ID.

## Definition of Done
- [x] `npm run check` and `npm run e2e` pass on the stub path with no behaviour change visible in the e2e specs beyond the footer string source. — `npm run check` green (19 files, 177 tests, 1 platform-skipped; typecheck + build); `npm run e2e` 31 passed; the only `e2e/chat.spec.ts` edits are the resume string now read from `stubProfile.resumeCommand()` and a `provider: stub` assertion on the written file.
- [x] Golden test: a task rendered from a fixed `WriteTaskRequest` with clock, session id and capture id stubbed equals the checked-in v0.1 golden file plus `provider: claude` and the Log suffix. — `test/tasks.test.ts` "golden task (PRD-providers M7 DoD, F-48)" against `test/fixtures/golden/task-v0.1.md`, recorded from the unchanged v0.1 code before any edit (commit 21d7acd).
- [x] `crt providers` on this repo prints `→ claude — .claude/, CLAUDE.md`; on an empty fixture root `→ claude (default)`; on a root with `.codex/` and Codex preflight failing, the "looks like codex" reason. — Run for real with the built CLI (see Log for the verbatim output) and pinned by `test/providers/detect.test.ts` ("this repo → …", "worked examples", "`.codex/` only with Codex not on PATH").
- [x] F-60 covers every worked example in F-44 and every layer in F-43, including `PUT`-replaces-flag (server-side function level; the route itself is CRT-0011). — `test/providers/detect.test.ts`: 17 tests over fixture roots × preflight stubs × launch env × config layers, exact reason strings; `ProviderRegistry.setActive()` is the PUT function.
- [x] The guard hook blocks an Agent SDK import in `providers/codex.ts` and allows it in `providers/claude.ts`; `prd-reviewer` no longer flags `provider:` as a key-order violation. — `test/guard-hook.test.ts` (blocks `providers/codex.ts` and `session.ts`, allows `providers/claude.ts`); `prd-reviewer.md` §6 now names `provider:` between `session:` and `tags:`, and the agent's run on this branch returned PR-READY WITH NOTES (test-title nits, since fixed).
- [x] v0.1 task files in `.crt/tasks/` still validate with `crt task <ID> --validate`. — `crt task CRT-0001|0003|0007|0009|0010|0014 --validate` all "is valid" on the built CLI; the dogfood test in `test/tasks.test.ts` validates every file in `.crt/tasks/`.

## Notes
Keep the move of `session.ts` as a pure rename in its own commit so the diff is reviewable. Do not add the codex `start()` here.

## Log
- 2026-09-15T17:30+08:00 — created from PRD-providers milestone M7 by the planning session that wrote docs/PRD-providers.md.
- 2026-09-15T19:53:49+08:00 — claimed by worker session 1ae79ebd-6306-4dec-b6d4-d31301c3f184
- 2026-09-15T20:37:34+08:00 — implemented on branch `crt/CRT-0010-m7-provider-abstraction-server` (worker session 1ae79ebd-6306-4dec-b6d4-d31301c3f184). Commits: pure rename `session.ts` → `providers/claude.ts` (own commit, as the Notes ask), golden fixture recorded from the pre-change code, guard-hook move (F-63), then the feature commit. Files: `packages/server/src/providers/{types,exec,claude,codex,stub,detect}.ts` (new/moved; `session-stub.ts` → `providers/stub.ts`), `session.ts` (now the registry: `listProviders`, `ProviderRegistry.resolve/setActive/refresh/status/payload`, `renderProviders`, `describeResolution`), `session-events.ts` (F-47 init shape, `ProviderCapabilities`, `SessionInfo.provider/nativeSessionId`), `sessions.ts` (provider per session, native id from init, provider-named log lines, a session that fails at once with the N-7 line), `tasks.ts` (`provider:` after `session:`, `session:` = native id, Log suffix, unknown keys ignored, `TaskSummary.provider`), `init.ts` (`provider`/`models`/`providers.<id>.command`, `.crt/config.local.json` layered over `.crt/config.json`, `crt init` gitignores the local file), `serve.ts` (`--provider`, preflight + detection at start, `CRT ready … provider: …`), `cli.ts` (`crt serve --provider`, `crt providers [--json] [--refresh]`, provider column in `crt tasks`), `packages/overlay/src/chat.ts` (footer from the init event only), `.claude/hooks/guard.mjs`, `.claude/agents/prd-reviewer.md`, `CLAUDE.md` (§9 lines), `plugin/skills/{tasks,task}/SKILL.md`, tests under `test/providers/` plus updates to `test/{tasks,sessions,session,project-init,guard-hook}.test.ts` and `e2e/chat.spec.ts`.
- 2026-09-15T20:37:34+08:00 — decisions taken without asking (F-37): (1) F-44 step 2 says "only claude passes → claude (default), no marker scan", but the F-45 sample (marked exact) prints `→ claude — .claude/, CLAUDE.md; codex not on PATH` in that very situation, so the marker scan always runs (it is one readdir) and the reason lists the winner's markers plus one note per unusable built-in (`codex not on PATH`, or `project looks like codex (.codex/) but codex is not on PATH` when pointed at); `(default)` is printed only when there is nothing to say. Consequence: on a machine without Codex an empty root prints `→ claude — codex not on PATH`, while with Codex usable it prints `→ claude (default)` (the DoD wording and the F-44 worked examples, which assume both agents usable). (2) The F-44 step-4 phrase omits the trailing " — using claude" because the line already starts with `claude —` (§4's own example has the same shape). (3) Reason strings not given by the PRD: launch context `started inside Claude (CLAUDECODE)`, ties `tie between claude (CLAUDE.md) and codex (.codex/)`, a non-default winner adds `; codex 0.154.0 logged in` (§4's sample). (4) `displayName` is the F-56 noun ("Claude"/"Codex"); the F-45 product line is a separate `agentName` ("Claude Code (Agent SDK)"/"Codex CLI"); `hints.install/login` carry the F-45 column text (N-7 says wording lives in the profile). (5) The F-45 column widths are the sample's and only widen when a value would not fit (a version, "not logged in", a problem line). (6) `crt providers --refresh` is accepted and changes nothing in the CLI (every CLI run is fresh; the cache it bypasses is the server's — the route is CRT-0011). (7) The F-47 `init` shape is exactly the PRD's, so `cwd` is gone from the event (the smoke test asserted it; it now asserts the ids/profile fields). (8) An unknown id in `--provider`/`CRT_PROVIDER` fails `crt serve` at start (`crt: --provider must be one of claude, codex (got "x")`); an unusable-but-known one starts the server with the N-7 line on the ready line and every session fails at once; an unknown or ACP-object provider in a config file does the same (the file may be shared with teammates on other versions). (9) The stub profile mirrors Claude (`displayName` "Claude", `resumeCommand` `claude --resume <id>`, Claude's capabilities) so the panel and e2e see no change; `session:` in its tasks is CRT's id (its native id). (10) `POST /__crt/sessions { provider }` is implemented at the function level (`SessionRegistry.create(captureId, { provider })`); the route field itself is F-57 / CRT-0011, as is `GET /__crt/providers`.
- 2026-09-15T20:37:34+08:00 — verification: `npm run check` green on Windows (typecheck both packages, 19 test files / 177 passed / 1 skipped — the POSIX sh-shim PATH case, which runs on ubuntu; the Agent SDK smoke test ran for real with the new init shape), `npm run build` ok, `npm run e2e` 31 passed, `claude plugin validate ./plugin` and `.` pass. Real CLI runs on this machine (Codex 0.154.0 installed via npm shim, `login status` exit 0; `CLAUDECODE` unset): this repo → `→ claude — .claude/, CLAUDE.md`; empty root → `→ claude (default)`; `.codex/` root with npm off PATH → `→ claude — project looks like codex (.codex/) but codex is not on PATH`; with `CLAUDECODE` set (as inside Claude Code) every case prints `→ claude — started inside Claude (CLAUDECODE)…` (F-44 step 1). `crt serve` ready lines observed: `provider: claude (default)`, `provider: codex (--provider)`, `provider: codex (CRT_PROVIDER) — codex not found on PATH — npm i -g @openai/codex, or set providers.codex.command in .crt/config.json`, `provider: stub (CRT_SESSION_STUB)`; `--provider nope` → exit 1 with the one-line error. `crt providers` row for this machine: `codex    ready        Codex CLI 0.154.0 …` — note `login status` exit 0 does not prove a usable token (M6 verdict), so the Codex driver (CRT-0012) must still map the turn-time auth error. For the reviewer: `serve()` lost the unused `sessionStarter` option and its handle gained `provider`/`providers`; the overlay footer now reads `session · model · <displayName> <agentVersion> · continue in a terminal: <resumeCommand>` and shows only `session <id>` until the init event arrives (M8 finishes F-47/F-56 on the overlay side); `.crt/tasks/README.md` was regenerated by `crt tasks`.

---
id: CRT-0028
title: M24 — Release 0.6.0: version bump and skill pins, PRD-polish §9/§10 evidence, tag, publish, promote
status: done
priority: normal
created: 2026-09-21T19:55:00+08:00
updated: 2026-09-22T15:18:00+08:00
url: null
route: null
session: null
tags: [m24, release, f-118, prd-polish]
files: [packages/server/package.json, plugin/.claude-plugin/plugin.json, .claude-plugin/marketplace.json, plugin/skills/, README.md, packages/server/README.md, docs/PRD-polish.md, docs/develop.md, CLAUDE.md, .github/workflows/release.yml]
---

## Summary
Ship v0.6.0: the brand (M20), the landing page (M21), the screenshots and illustrations (M22), the README front page and docs split (M23) and the still-unreleased Antigravity profile (PRD-providers M19, CRT-0023). Bump the version everywhere the pin test looks, apply the PRD-polish §9 rows that M20–M23 did not, tick §10 with evidence, tag, let the release workflow stage the package, and have Simon promote it on npm. `docs/PRD-polish.md` F-118.

## Context
Release recipe (`CLAUDE.md` › Commands › Release; `docs/develop.md` after M23): bump `version` in `packages/server/package.json`, `plugin/.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`, and the pinned `claude-review-tool@<major.minor>` in the seven skills (`test/skill-pin.test.ts` fails when they disagree); merge; `git tag v0.6.0 && git push origin v0.6.0`. The `release` workflow fails if tag ≠ package version, runs `npm run check`, stages the version on npm via trusted publishing (no token) and creates a GitHub Release with generated notes; the version is live only after Simon promotes it on npmjs.com (package → Versions, 2FA) — that step is his. The SessionStart hook compares the plugin's major.minor with the project's `node_modules/claude-review-tool` (F-84/F-106): after promotion, `npm i -D claude-review-tool@0.6.0` in the trial app (`C:\Projects\Claude\tool-validation`) and `crt setup` must agree. The npm page renders `packages/server/README.md`; the lockup there is an absolute raw.githubusercontent URL (M23). 0.5.0 is the latest published version; `main` already carries M19.

## Evidence
No page capture: created from `docs/PRD-polish.md` milestone M24 by the planning session.

## Ask
1. Versions: `0.6.0` in the three manifests; `claude-review-tool@0.6` in the seven skills; the README's version reference; `crt --version` prints `crt 0.6.0 (agent sdk <pinned>)`.
2. PRD-polish §9: apply any row not yet applied by M20–M23 (the `CLAUDE.md` Release line: "re-run `npm run screenshots` when the overlay's UI changed since the last release"); mark each row's milestone in the table.
3. PRD-polish §10: tick every row with evidence (test names, e2e specs, task Log entries, run URLs) — the M18 pattern in `docs/PRD-embedded.md` §10.
4. PRD-polish status table → "Built — M20–M24 landed (CRT-0024…0028, dates)"; decision 1 records the direction that shipped.
5. Tag `v0.6.0` after the merge; confirm the release workflow run is green and the GitHub Release exists with notes naming M19 (Antigravity), M20 (brand), M21 (landing page), M22 (screenshots), M23 (README and docs); then ask Simon to promote (user-only: 2FA on npmjs.com).
6. After promotion: `npm view claude-review-tool version` → `0.6.0`; in a scratch profile `npm i -g claude-review-tool@0.6.0 && crt setup` installs `crt@crt 0.6.0`; the trial app updated to the published 0.6.0 and its SessionStart hook silent on drift; the npm page shows the lockup and the Install block.

## Definition of Done
- [x] `test/skill-pin.test.ts` green at `0.6.0` / `@0.6`; `npm run check` and `npm run e2e` green on the release commit.
- [x] Every PRD-polish §9 row applied and every §10 row ticked with evidence; the status table updated.
- [x] `v0.6.0` tagged on `main`; release workflow green; GitHub Release notes name M19–M23.
- [x] Manual (Simon): the staged version promoted on npmjs.com with 2FA — the one user-only step.
- [x] After promotion (worker session): `npm view` shows `0.6.0`; a clean global install + `crt setup` reports `crt@crt 0.6.0`; the trial app on the published package shows no drift line; the npm page renders the lockup.

## Notes
Depends on CRT-0024, CRT-0025, CRT-0026 and CRT-0027 being `done` (merged). Do not tag before the merge; do not `--admin`-merge (memory: merge PRs with a bare `gh pr merge <n> --squash --delete-branch`). If the release workflow fails on the tag/version check, fix the version, re-tag on the fixed commit (`git tag -d`, `git push --delete origin v0.6.0`, re-tag) and say so in the Log.

## Log
- 2026-09-21T19:55+08:00 — created from docs/PRD-polish.md milestone M24 by the planning session that wrote it (session 78b6555f-fedb-4424-b1ab-1d8477f99af8).
- 2026-09-22T14:21+08:00 — claimed by /crt:next, session 8bfe6979-6e2d-4bd5-9690-de7e6b25ab14, branch crt/CRT-0028-m24-release-0-6-0
- 2026-09-22T14:30+08:00 — read PRD-polish §8–§10, §13, the M18 pattern in PRD-embedded §9/§10, the Logs of CRT-0024…0027 and CRT-0029 (the evidence), the 0.5.0 bump commit (3a827f8) and the 0.4.0 one (91252db) for what a bump touches, and CRT-0022's Log for how the release half of a release task is handled (the tag/release rows stay open at PR time and are filled after the merge). Reproduced: `npx --no crt --version` → `crt 0.5.0 (agent sdk 0.3.270)`; none of the PRD-polish §9 rows had a `*v0.6: …*` note in the four earlier PRDs (grep `v0.6` / `PRD-polish` in docs/PRD{,-providers,-setup,-embedded}.md: 0 hits); the prd-reviewer did not read PRD-polish.
- 2026-09-22T14:35+08:00 — decisions: (1) the sample versions in the doctor/setup/hook lines move one step, as the 0.5.0 bump did — `held by CRT 0.6.0`, `crt@crt 0.5.0 installed, this is 0.6.0`, `installed crt@crt 0.6.0`, `plugin 0.6.0 but the project's claude-review-tool is 0.5.0` — in README.md, docs/troubleshooting.md and the docs.test.ts rows that pin them; the `<!-- BEGIN:crt v0.5 -->` stamp quoted in README.md and docs/troubleshooting.md reads `v0.6` (init.ts stamps the running major.minor); `landing.test.ts`'s fixture rows (already `0.5.0 … this is 0.6.0`) are test data and stay. (2) README line 5: `v0.6.0.` and the clause `v0.6 ships PRD-polish M20–M23 (…) and PRD-providers M19 (the Antigravity CLI)`, keeping the v0.5 clause as history (`docs.test.ts` pins `v0.6.0.` and the PRD-embedded link on that line). (3) The §9 rows are applied inline to the earlier PRDs as `*v0.6: …*` notes, the way M18 wrote the `*v0.4: …*` notes (PRD.md: the header's Amended-by row gains PRD-polish, F-65, §8, §9, §11's README row; PRD-providers.md: F-62, F-64, the §10 N-7 row; PRD-setup.md: F-76, F-78, F-80, F-88, N-16, N-17; PRD-embedded.md: F-91, F-100, F-107), and the §9 table says which milestone's code applied each row and that M24 wrote the note. (4) Two hand-offs the earlier Logs left for M24 are recorded: Re-check's `/__crt/providers?refresh=1` (CRT-0025 decision b, flagged by the prd-reviewer as beyond F-114's literal list) as an inline note in F-114 and a §9 row; §5.3's "the stub's fixed session ids" (CRT-0026: ids are server-minted) as an inline note. (5) `.claude/agents/prd-reviewer.md` reads PRD-polish §6, §7, §9 and knows where F-111 / F-112…F-118 / N-23…N-27 live — the twin of the CLAUDE.md read-line row, as M18 did for PRD-embedded; noted in that §9 row. (6) `docs/develop.md` › Release gets the same screenshots clause as CLAUDE.md (it mirrors that line; no doc test pins it). (7) No screenshot regeneration: `git log e44e7a2..HEAD -- packages/overlay` is empty — nothing in the overlay changed since CRT-0029 took the committed PNGs — so the Release line's condition does not apply to this release.
- 2026-09-22T14:40+08:00 — verified: DoD 1 — `npx vitest run test/skill-pin.test.ts test/docs.test.ts test/init-skill.test.ts test/docs-images.test.ts` 133/133 (one docs.test.ts row first failed on the `<!-- BEGIN:crt v0.6 -->` stamp still reading `v0.5` on docs/troubleshooting.md — fixed); `npm run check` green — typecheck both packages, 50 files / 683 tests passed + 1 skipped, build (`built marketplace crt 0.6.0 -> packages/server/dist/plugin-marketplace`); two earlier full runs each lost one unrelated case to a known Windows flake (`test/intake-skill.test.ts`'s copy-intake run alongside the build, then `test/providers/antigravity.test.ts`'s interrupt/resume case — the same file that flaked on PR #74's first CI attempt and in CRT-0029's runs), each green alone and on the third full run; nothing under packages/server/src or packages/overlay changes on this branch. `claude plugin validate` passes on ./plugin, ., packages/server/dist/plugin-marketplace and packages/server/dist/plugin-marketplace/plugin. `node packages/server/dist/cli.js --version` → `crt 0.6.0 (agent sdk 0.3.270)`. `npm run e2e` (`npx playwright test --workers=2`, the Windows note): 62 passed in 1.4 m, every spec including landing/proxy/embedded/start.
- 2026-09-22T14:40+08:00 — verified: DoD 2 — PRD-polish §9: every row carries its milestone; the 12 rows that amend the earlier PRDs now have their `*v0.6: …*` note in place (grep `v0.6`: docs/PRD.md 5, PRD-providers.md 3, PRD-setup.md 6, PRD-embedded.md 3); the CLAUDE.md Release row is applied (CLAUDE.md line and docs/develop.md › Release: `re-run npm run screenshots when the overlay's UI changed since the last release`); §10: rows 1–7 ticked with test names, e2e titles, Log stamps and PR numbers from CRT-0024…0027 / CRT-0029; row 8 (CI on main, the tag, the release, the npm page) stays open with a pending note until this task's close-out; the status table reads `Built — M20–M24 landed …`; decision 1 records what shipped (files, geometry, where the mark is used).
- 2026-09-22T14:42+08:00 — pre-merge dry run of the Ask 6 install path from the packed tarball (`npm pack` → claude-review-tool-0.6.0.tgz, 119 files; the tarball stays untracked next to the older ones): `npm i -g --prefix <scratch>` then, with a fresh `CLAUDE_CONFIG_DIR`, `crt --version` → `crt 0.6.0 (agent sdk 0.3.270)`, `crt setup` → `registered marketplace crt from <prefix>/node_modules/claude-review-tool/dist/plugin-marketplace` and `installed crt@crt 0.6.0 — restart Claude Code to load /crt:serve, /crt:next, /crt:tasks, /crt:task, /crt:done, /crt:intake, /crt:init`; `claude plugin list --json` → `crt@crt 0.6.0` with the seven skills under `plugins/cache/crt/crt/0.6.0/skills`; a second `crt setup` → `crt setup: crt@crt 0.6.0 is already installed`. The same steps run again from the published package after promotion (DoD 5).
- 2026-09-22T14:44+08:00 — prd-reviewer (subagent) on the branch: PR-ready — every hunk traceable to F-118 and the §9 rows, no source file changed, no invariant breached (no routes, network, writes, spawns or SDK imports on added lines; the SDK pin exact; the lock diff one line), no test titles added; one pre-existing labelling quirk fixed on the spot (the §9 row said `PRD F-64`; F-64 is PRD-providers'), and a hygiene note that the four untracked `packages/server/claude-review-tool-*.tgz` are not gitignored — left as the earlier sessions left them, unstaged.
- 2026-09-22T14:45+08:00 — ready for review: changed packages/server/package.json, plugin/.claude-plugin/plugin.json, .claude-plugin/marketplace.json, package-lock.json (the workspace entry), plugin/skills/{done,init,intake,next,serve,task,tasks}/SKILL.md (`@0.6`), packages/server/test/{docs,init-skill}.test.ts, README.md, docs/{cli,install,troubleshooting,develop}.md, docs/PRD-polish.md, docs/{PRD,PRD-providers,PRD-setup,PRD-embedded}.md (the `*v0.6: …*` notes), CLAUDE.md, .claude/agents/prd-reviewer.md. No source change; the bump is the last code commit on the branch (CRT-0022's Notes). DoD rows 3–5 stay open by design (this task's Notes: never tag before the merge): after the PR merges this session tags `v0.6.0`, watches the `release` workflow, checks the Release notes name M19–M23, then hands the promote step to Simon (2FA on npmjs.com) and finishes DoD 5 from the published package. `packages/server/claude-review-tool-{0.3.0,0.4.0,0.5.0,0.6.0}.tgz` are untracked and not staged.
- 2026-09-22T14:58+08:00 — PR #80 CI: changes, check (ubuntu-latest, windows-latest), e2e (ubuntu), CodeQL (javascript-typescript, actions) all green on the first attempt; squash-merged as dbb5f41 (bare `gh pr merge 80 --squash --delete-branch`, main fast-forwarded).
- 2026-09-22T15:05+08:00 — verified: DoD 3 — tag `v0.6.0` on dbb5f41 (the merge commit on `main`), pushed; the `release` workflow run https://github.com/simv/crt/actions/runs/35696378582 succeeded (checkout, Node 22 + npm 11, `npm ci`, `npm run check`, the tag = package version step, `npm stage publish --workspace packages/server --access public --provenance`: `+ claude-review-tool@0.6.0 (staged with id 5ca1d973-6fc0-4be4-9d7b-9ae60fc95620)`, 117 files, 272.4 kB packed, provenance in the Sigstore log at logIndex 2908216288, staged with tag `latest`); GitHub Release https://github.com/simv/crt/releases/tag/v0.6.0 exists and is Latest. The generated notes listed every PR since v0.5.0 (#60–#80) but named no milestone by number, so a summary was prepended with `gh release edit --notes-file`: one line each for M19 (Antigravity, #61), M20 (the mark, #68), M21 (the landing page, #70), M22 (screenshots and illustrations, #72 + #77), M23 (README front page, #74) and M24 (#80), the F-65 clamp and the CI docs-only skip, and the upgrade commands; the generated list stays beneath it. `npm view claude-review-tool version` is still `0.5.0` (`dist-tags.latest` 0.5.0 — so 0.5.0 was promoted after all): 0.6.0 waits on Simon's promotion (DoD 4).
- 2026-09-22T15:10+08:00 — verified: DoD 4 — Simon promoted 0.6.0 on npmjs.com ("promoted 0.6.0 on npm — finish it up"); `npm view claude-review-tool version` → `0.6.0`, `dist-tags.latest` = 0.6.0, versions 0.1.0 / 0.3.0 / 0.4.0 / 0.5.0 / 0.6.0; `dist.integrity` `sha512-0L0g6r/h6baGEPyxTf9yYblx3PGoydA8W5CYVkR8LZQuxyhCx9K5pQl5ovAD1EyRulJr+0xg6e+vqvpY/yQocQ==` equals the shasum/integrity the release run staged (117 files).
- 2026-09-22T15:15+08:00 — verified: DoD 5 — (1) clean install from the registry into a fresh `npm --prefix` and a fresh `CLAUDE_CONFIG_DIR`: `npm i -g claude-review-tool@0.6.0`; `crt --version` → `crt 0.6.0 (agent sdk 0.3.270)`; `crt setup` → `registered marketplace crt from <prefix>/node_modules/claude-review-tool/dist/plugin-marketplace` and `installed crt@crt 0.6.0 — restart Claude Code to load /crt:serve, /crt:next, /crt:tasks, /crt:task, /crt:done, /crt:intake, /crt:init`; a second `crt setup` → `crt setup: crt@crt 0.6.0 is already installed`; `claude plugin list --json` → `crt@crt 0.6.0` with the seven skills under `plugins/cache/crt/crt/0.6.0/skills`. (2) The trial app C:\Projects\Claude\tool-validation: `npm i -D claude-review-tool@0.6.0` replaced the 0.5.0 tarball devDependency (`^0.6.0`, node_modules 0.6.0; its package.json / package-lock.json changes are Simon's, uncommitted there as before); Simon's global `crt` updated the same way (`npm i -g claude-review-tool@0.6.0` → 0.6.0) and `crt setup` on his profile → `crt setup: updated crt@crt 0.6.0 — restart Claude Code …`, `claude plugin list` → `crt@crt 0.6.0`. The installed plugin's hook run against the trial app (`CLAUDE_PROJECT_DIR=C:\Projects\Claude\tool-validation node <cache>/crt/crt/0.6.0/hooks/session-start.mjs`) printed only the backlog line (`CRT: 17 of 17 tasks in backlog …`) — no F-84 drift line; `crt doctor` there: `ok    plugin    crt@crt 0.6.0 installed (claude on PATH)`, `ok    target    http://localhost:3100 (remembered) — responding` (a first run said `warn … not responding` while Next recompiled after the install), `ok    integration next — app/layout.tsx imports claude-review-tool/react`, and `FAIL  port      4400 held by CRT 0.5.0 → http://localhost:3100 (this project) — crt --replace` — that is Simon's own review server from before the release (3 open sessions), left running; restart it on 0.6.0 when convenient (`crt --replace` in the trial app). (3) The npm page https://www.npmjs.com/package/claude-review-tool: `0.6.0 • Public • Published 15 minutes ago`, the lockup rendered from `https://raw.githubusercontent.com/simv/crt/main/docs/brand/crt-lockup.svg` (naturalWidth 300 × 113, the URL answers 200 `image/svg+xml`, 896 bytes) and the Install block (`npm i -g claude-review-tool`, `crt setup`) in the README — measured in the built-in browser and with Playwright (`.crt/captures/npm-page-CRT-0028.mjs`, headed: npm's page serves a Cloudflare challenge to curl); screenshot `.crt/tasks/assets/CRT-0028/npm-page.png`. The registry document's `readme` for 0.6.0 carries the same lockup URL and the block.
- 2026-09-22T15:18+08:00 — PRD-polish §10 row 8 ticked with the tag / run / Release / npm evidence; v0.6 definition of done complete. done; merged in https://github.com/simv/crt/pull/80

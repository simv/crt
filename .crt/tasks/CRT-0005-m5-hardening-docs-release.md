---
id: CRT-0005
title: M5 — Hardening, Should-items, docs and v0.1.0 release
status: done
priority: normal
created: 2026-09-14T20:50:00+08:00
updated: 2026-09-15T13:32:00+08:00
url: null
route: null
session: null
tags: [m5, release, docs]
files: [README.md, .github/workflows/release.yml, packages/server/package.json]
---

## Summary
Close out the PRD definition of done: pick the highest-value Should items, write the user docs and troubleshooting, and publish `claude-review-tool@0.1.0` through the release workflow.

## Context
PRD §11 (project DoD), §6 Should items (F-5, F-6, F-12 sessionStorage, F-14 quick note, F-21, F-22, F-29, F-30, F-40, F-41), milestone M5. Depends on CRT-0004. `release.yml` needs the `NPM_TOKEN` repo secret, which Simon adds manually.

## Evidence
None — greenfield task from the PRD.

## Ask
1. Implement, in value order and within budget: F-14 quick note, F-6 script-tag mode with localhost-only CORS, F-21 failed network requests, F-30 session list. Record what was deferred and why in the Log.
2. README: install, the loop, task format, script-tag fallback, troubleshooting for every N-6 case, and a short "how it works" section.
3. Walk PRD §11 and tick each item with evidence (test name, or manual note in this Log).
4. Bump `packages/server` to 0.1.0, tag `v0.1.0`, confirm the release workflow publishes and creates a GitHub Release.

## Definition of Done
- [x] Every PRD §11 checkbox is ticked with a pointer to evidence. (All 12 ticked in `docs/PRD.md` with evidence inline; item 10 ticked after the release, see Log 13:32.)
- [x] `claude-review-tool@0.1.0` is on npm; `npx claude-review-tool@0.1.0 serve --open` works in the trial project. (`npm view claude-review-tool version` → 0.1.0; run from the Apex root with no `--target`: probe found :3000, `crt init` ran, ready in 5.6 s, launcher present, Next 15.5.25 detected, HMR WebSocket through :4400, tag injected — see Log 13:32.)
- [x] GitHub Release v0.1.0 exists with generated notes. (https://github.com/simv/crt/releases/tag/v0.1.0, notes list PRs #1–#11.)
- [x] README covers everything listed in the Ask. (Install, The loop, `crt serve` flags, What Claude gets, Task format, Script-tag fallback, How it works, Troubleshooting for every N-6 case + "Overlay does not appear", Repository, Develop incl. the release procedure.)

## Notes
—

## Log
- 2026-09-14T20:50+08:00 — created from PRD milestone M5 during project setup.
- 2026-09-15T12:32+08:00 — claimed by build session (Claude Code, VS Code); branch `crt/CRT-0005-m5-hardening-docs-release`. Found at claim time: `claude-review-tool` still 404 on npm; `gh secret list` shows no `NPM_TOKEN` yet (Simon adds it); `main` ruleset requires PR + `check (ubuntu-latest)`, `check (windows-latest)`, `e2e (ubuntu)` and squash merges; no releases or tags exist. F-5, F-12 (sessionStorage), F-22, F-29, F-40, F-41 were already delivered by M1–M4, so the Should work here is F-14, F-6, F-21, F-30 in that order.
- 2026-09-15T12:58+08:00 — build session (Claude Code, VS Code): Should items, docs and release prep done; verification below.
  - **F-14 quick note.** Toolbar gains **Quick note** next to Send, enabled only when every annotation has a note (no conversation to add words later). `POST /__crt/sessions { quick: true }` (warm start keeps the flag) → the first message ends with `QUICK_NOTE_INSTRUCTIONS` (intake-message.ts: decide the DoD yourself, call `write_task` directly, ask only if the note + code cannot say what is wanted); `SessionInfo.quick`. The panel *follows* the session hidden (`ChatPanel.follow`) and reports through `onQuiet`: `task_written` → status line "Task CRT-NNNN written to … [open chat]"; `idle` without a task, `error`, `ended`, or a permission card → the panel opens itself with "Claude needs you: …". `plugin/skills/intake/SKILL.md` step 4 documents the mode (single source, F-39). Stub gained the quick script (write directly; "ask me" in the note → question). Tests: `test/sessions.test.ts` ×2, `test/intake-message.test.ts`, `e2e/chat.spec.ts` "Quick note" ×2. **Live run with the real SDK** (fixture app behind `crt serve` from this repo root, driver script through `window.__crt`, note about the Widget price): session started 172 ms after Send, `CRT-0007` written **46 s** later — 7 tool calls (Read capture.json, Read server.mjs, ToolSearch, Grep ×2, Read capture.spec.ts, write_task), zero questions, zero permission prompts, panel never opened, status line showed the id, `GET /__crt/sessions` listed `quick: true` with the note as summary. The written file (validated by `createTask`) had exact `file:line` references, a 6-item DoD and a Notes scope decision — a workable task from one note. Trial task and assets removed afterwards.
  - **F-6 script-tag mode.** `applyCors` in http.ts: every `/__crt/` response carries `access-control-allow-origin: <origin>` (+ methods GET/POST/DELETE/OPTIONS, headers content-type/last-event-id, max-age 600, `vary: origin`) when the request's `Origin` is `localhost`, `*.localhost`, `127.0.0.1` or `[::1]` (any port, http/https, `url.origin === origin` so a path or trailing slash never matches); OPTIONS → 204 for those, 403 for anything else, and no header at all otherwise. Overlay `base.ts` reads `document.currentScript.src` while the bundle evaluates and prefixes `crtUrl()` onto every fetch and the `EventSource`. Fixture route `/script-tag?crt=<origin>`. Tests: `test/proxy.test.ts` "script-tag mode CORS (F-6)" ×2 (allowed origins, preflight, the deny list incl. `http://localhost.evil.example`); `e2e/script-tag.spec.ts` loads the fixture straight from :3999 with the tag pointing at :4499 and runs a full Send — preflighted JSON POST, session POST, SSE stream — cross-origin, first message shows `Page: http://localhost:3999/script-tag`, no console errors. README "Script-tag fallback" + a troubleshooting entry.
  - **F-21 failed requests.** `network-hook.ts` (bundled into early.js and overlay.js like the console hook, buffer on `window.__crt.__network`): `fetch` wrapper (observes on a branch so the caller's chain is untouched), `XMLHttpRequest.open/send` wrapper (`loadend`, status ≥ 400 or 0), and a `PerformanceObserver` on `resource` entries with `responseStatus ≥ 400` for img/script/css (buffered, so script-tag mode sees earlier failures); `/__crt/` and `data:`/`blob:` URLs skipped; ring of 50. Schema: `NetworkEntrySchema` + required `bundle.network` (`MAX_NETWORK_ENTRIES`), snapshot taken *before* rasterisation so the screenshot pass's own fetches never appear. First message line "Failed network requests since load: N — first: …"; Evidence line "Failed requests at send time: …". Fixture `/app` now fires a 404 fetch, a 404 XHR POST and a missing image. Tests: `test/capture-schema.test.ts` ×2, `test/intake-message.test.ts`, `test/tasks.test.ts`, `e2e/capture.spec.ts` "failed network requests (F-21)" (fetch/xhr/resource all recorded, none of CRT's own).
  - **F-30 session list.** `SessionInfo` gains `summary` (first non-empty note, else "N annotations on /path", ≤ 80 chars) and `url`, filled when the capture is attached; `GET /__crt/sessions` was already newest-first. Toolbar **Sessions** button opens a list panel (note, state pill or task id, time · quick note · path · id prefix); a row re-opens that session in the chat; current session highlighted. Tests: `test/sessions.test.ts` (fields, ordering, warm start has no summary), `e2e/chat.spec.ts` "Sessions lists recent intake sessions newest first and re-opens one (F-30)".
  - **Deferred (Should items not taken).** None of the four in the Ask were deferred. Of the other Should items, F-5, F-12 sessionStorage, F-22, F-29, F-40 and F-41 were already delivered by M1–M4. Not done, out of the Ask's list: surfacing thinking summaries in the panel (noted as a possible M5 item in CRT-0003's Log; not a PRD requirement, so it needs a PRD change first) and the PRD §13 priority toggle on Send (open question 3; `write_task` already takes `priority`, so intake can set it from the note).
  - **Release prep.** `packages/server` → 0.1.0 with `homepage`, `repository` (`git+https://github.com/simv/crt.git`, `directory: packages/server` — required for `npm publish --provenance`), `bugs`, `keywords`; plugin.json, marketplace.json and the private overlay package → 0.1.0 too. New `packages/server/README.md` (short, links here) and the repo LICENSE copied into the package dir at build (gitignored) so npm bundles both. `npm pack --dry-run`: 42 files, 91 kB packed / 345 kB unpacked. `release.yml` unchanged: tag `v*` → `npm ci` → `npm run check` → version-equals-tag guard → `npm publish --access public --provenance` → GitHub Release with generated notes. **Blocked on Simon:** the `NPM_TOKEN` repo secret does not exist yet (`gh secret list` is empty). Once it does: merge this PR, then `git tag v0.1.0 && git push origin v0.1.0` from `main`, then `npx claude-review-tool@0.1.0 serve --open` in the trial project and tick PRD §11 item 10 + DoD items 1–3 here.
  - **README.** Rewritten around the verified Troubleshooting section (kept byte-for-byte from CRT-0006, plus an "Overlay does not appear" entry pointing at script-tag mode): Install, The loop (tools, Send, Quick note, Sessions, permissions, `/crt:*`), `crt serve` flags, What Claude gets, Task format (full F-32 example + rules), Script-tag fallback, How it works (proxy, overlay, intake session, permissions, privacy), Repository, Develop + release procedure.
  - **PRD §11 walk** — every Must requirement and its evidence: F-1 `test/target.test.ts`, `test/project-init.test.ts`; F-2 `test/inject.test.ts`, `test/proxy.test.ts`, `e2e/proxy.spec.ts`; F-3 `test/proxy.test.ts` + `e2e/proxy.spec.ts` (WebSocket echo) + CRT-0001 Log (Next 15.5 HMR on the trial app); F-4 `test/proxy.test.ts`, `e2e/proxy.spec.ts`; F-7…F-12 `e2e/capture.spec.ts` "launcher and tools"; F-13 `test/captures.test.ts`, `test/proxy.test.ts`, `e2e/capture.spec.ts`; F-15…F-17, F-19, F-20 `test/capture-schema.test.ts` + `e2e/capture.spec.ts`; F-18 `e2e/capture.spec.ts` (React 18 fiber walk), `test/owner-stack.test.ts` (React 19), CRT-0002 Log (trial app); F-23 `test/captures.test.ts` (prune), `test/tasks.test.ts` (asset move), `e2e/chat.spec.ts`; F-24 `test/intake-message.test.ts`, `test/session.test.ts`, `test/sessions.test.ts`; F-25 `test/sessions.test.ts`, `e2e/chat.spec.ts`; F-26 `test/permissions.test.ts`, `e2e/chat.spec.ts` (Deny/Allow); F-27 manual — CRT-0003 Log (1) on the trial app and the live quick-note run above; F-28 `test/session.test.ts`, `e2e/chat.spec.ts`; F-31…F-34 `test/tasks.test.ts` (+ `e2e/chat.spec.ts` for the file on disk); F-35 `test/project-init.test.ts`; F-36…F-39 manual — CRT-0004 Log (skills reconciled, `/crt:next` on simv/crt#7 and simv/apex#257, fresh-profile install) + CI step `claude plugin validate`; N-6 `test/target.test.ts`, CRT-0001 Log (three CLI cases), `test/session.test.ts`/`session.ts` `loginProblem`. PRD §11 items 1–9, 11, 12 ticked in `docs/PRD.md` with these pointers; item 10 open pending the tag.
  - **Verification.** `npm run check` green on Windows (Node 24.18): typecheck both packages, **122 unit tests** in 14 files (8 new; incl. the SDK smoke test locally), overlay build 85.3 KB raw / 27.6 KB gzipped + early.js 3.5 KB / 1.5 KB (N-3), server build. `npm run e2e` green: **30 Playwright tests** (5 new). Live F-14 run above. `claude plugin validate ./plugin` and `.` pass (run in CI).
- 2026-09-15T13:10+08:00 — merged as c89091b (https://github.com/simv/crt/pull/10); Simon added `NPM_TOKEN`. First `v0.1.0` tag push failed at `npm publish`: `E422 Error verifying sigstore provenance bundle: Unsupported GitHub Actions source repository visibility: "private"` — npm only attests builds from public repos. Fix (https://github.com/simv/crt/pull/11, 582c8c7): dropped `--provenance` and `id-token: write` from `release.yml` with a comment on re-enabling them if the repo goes public; `npm pkg fix` cleaned the `bin` path npm had warned about. Tag deleted and re-created at 582c8c7 (nothing had been published).
- 2026-09-15T13:32+08:00 — release run 34932661658 green: `claude-review-tool@0.1.0` on npm (tarball https://registry.npmjs.org/claude-review-tool/-/claude-review-tool-0.1.0.tgz), GitHub Release v0.1.0 with generated notes. Trial-project check from the published package only: `next dev` on :3000 in `C:\Projects\Claude\Apexpps\web`, then `npx -y claude-review-tool@0.1.0 serve --open` from `C:\Projects\Claude\Apex` with no `--target` → `crt init: created .crt/tasks, .crt/config.json, .gitignore`, `CRT ready at http://localhost:4400 → http://localhost:3000 (project: C:\Projects\Claude\Apex, 0 tasks)` 5.6 s after launch (incl. the npx download); Chromium on :4400 → `#crt-host` with the launcher, `framework: next 15.5.25 (webpack)`, one WebSocket `ws://localhost:4400/_next/webpack-hmr`, `<script src="/__crt/overlay.js" defer>` in the gzip-negotiated HTML. Apex restored afterwards (`.crt/` removed, `.gitignore` reverted, `git status` clean), servers killed. PRD §11 item 10 ticked; all DoD items ticked. Status → done.
